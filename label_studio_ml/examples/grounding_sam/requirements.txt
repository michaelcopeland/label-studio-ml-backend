# 1. CORE COMPATIBILITY PINS
# Use latest compatible versions - PyTorch 2.4 supports NumPy 2.x
numpy

# Pin transformers to avoid _pytree error
transformers==4.36.2

# Use OpenCV compatible with NumPy 2.x
opencv-python-headless

# Pin accelerate to a stable version compatible with these
accelerate==0.20.3

# 2. APPLICATION DEPENDENCIES
# Pin the SDK to 2.0.0 FIRST to avoid the recent OpenCV 4.12 requirement
label-studio-sdk==2.0.0

# label-studio-ml is copied locally in Dockerfile

# Segment Anything Model
segment-anything==1.0

boto3>=1.26.103,<2.0.0
openmim~=0.3.9
mmcv>=2.0.0rc4,<2.2.0

# Add flask for the API
flask

# Add semver for the local code
semver

# Add colorama for the local code
colorama

gunicorn==23.0.0
olefile==0.47

rq
